# Git & Version Control Policy

- **No Automatic Staging, Committing, or Pushing**: Do NOT run `git add`, `git commit`, or `git push` automatically after making code changes unless the user explicitly requests you to perform a git commit or push.
